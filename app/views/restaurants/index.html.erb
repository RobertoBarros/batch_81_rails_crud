<%= link_to 'New Restaurant', new_restaurant_path %>


<h1>Todos os restaurantes</h1>

<% @restaurants.each do |restaurant| %>

  <p>name: <%= link_to restaurant.name, restaurant_path(restaurant) %>
  <p>address: <%= restaurant.address %>
  <hr>

<% end %>